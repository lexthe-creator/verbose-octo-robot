// Step 4 — built next
export default function Home({ onOpenFocus, onOpenInbox }) {
  return (
    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', minHeight: '60dvh', color: 'var(--color-muted)', fontFamily: 'var(--font-body)' }}>
      Home — coming soon
    </div>
  )
}
